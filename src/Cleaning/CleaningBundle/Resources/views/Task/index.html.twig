{% extends '::base.html.twig' %}

{% block body -%}
    <h2>Uppgifter</h2>
     {% for entity in entities %}
    <ul class="entity-list">
        <li><a href="{{ path('task_show', { 'id': entity.id }) }}">{{ entity.name }}</a> <a href="{{ path('task_show', { 'id': entity.id }) }}">Visa</a> <a href="{{ path('task_edit', { 'id': entity.id }) }}">Ändra</a> <a href="{{ path('task_delete', { 'id': entity.id }) }}">Ta bort</a></li>
    </ul>
    {% endfor %}       
    <p><a href="{{ path('task_new') }}">
        Lägg till uppgift
    </a></p>   
{% endblock %}
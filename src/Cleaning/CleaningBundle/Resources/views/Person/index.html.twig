{% extends '::base.html.twig' %}

{% block body -%}
    <h2>Personer</h2>
     {% for entity in entities %}
    <ul class="entity-list">
        <li><a href="{{ path('person_show', { 'id': entity.id }) }}">{{ entity.name }}</a> <a href="{{ path('person_show', { 'id': entity.id }) }}">Visa</a> <a href="{{ path('person_edit', { 'id': entity.id }) }}">Ändra</a> <a href="{{ path('person_delete', { 'id': entity.id }) }}">Ta bort</a></li>
    </ul>
    {% endfor %}       
    <p><a href="{{ path('person_new') }}">
        Lägg till person
    </a></p>   
{% endblock %}
